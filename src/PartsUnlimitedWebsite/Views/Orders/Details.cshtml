@model PartsUnlimited.ViewModels.OrderDetailsViewModel
@{
    if (Model == null)
    {
        ViewBag.Title = "Unknown order";
    }
    else
    {
        ViewBag.Title = $"Order history for {Model.Order.OrderId}";
    }
}

@await Html.PartialAsync("_Order", Model)